# Порт, на котором API публикуется на localhost (если 8000 занят другим проектом)
API_PORT=8000
DATABASE_URL=postgresql://honest_month:local_dev_password@localhost:5432/honest_month
POSTGRES_PASSWORD=local_dev_password
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
TEXT_MODEL_MODE=disabled
# TEXT_MODEL_URL=https://example.invalid/text-suggestions
# TEXT_MODEL_NAME=your-model
# TEXT_MODEL_API_KEY=replace-me

# Адрес API для фронтенда (Vite читает из .env.local)
VITE_API_URL=http://127.0.0.1:8000
